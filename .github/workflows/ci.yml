name: CI

on:
oull_request:
branches: [ main ]

jobs:
testes:
runs-on: ubuntu-latest
steps:
- uses: actionc/checkout@v4
- name: Instalar depenências
run: npm install
- name: Rodar os testes
run: npm test